from dataclasses import dataclass
from typing import TypedDict, Union

from stw_gen_sample3_api.basic_types import *
from stw_py_api_runtime.gen_classes.telemetry.parse import Message as _Message


from stw_gen_sample3_api.types.core.service.types.sha_2_5_6_type import Sha256


@dataclass
class Sha256Result(_Message):

    # Адрес
    address: uint32_t
    # Длина
    length: uint32_t
    # Результат расчёта SHA256
    sha256: Sha256


class Sha256ResultDict(TypedDict):

    # Адрес
    address: Union[uint32_t, int ]

    # Длина
    length: Union[uint32_t, int ]

    # Результат расчёта SHA256
    sha256: Union[Sha256]

