from stw_py_api_runtime.gen_classes.types.enum_type import EnumType
from stw_gen_sample3_api.basic_types import *


class Reason(EnumType):
    """Причины смены режима работы"""

    @classmethod
    def _base_type(cls):
        return uint8_t

    # Старт устройства
    start = 0
    # Принудительная смена состояния
    cmd = 1
