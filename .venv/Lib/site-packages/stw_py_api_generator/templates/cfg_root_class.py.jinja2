{% for next_name in next_levels %}
from .next_level import {{ next_name }}
{% endfor %}


class Cfg:
    def __init__(self, device, broker):
        self.device_id = device.device_id
        self.broker = broker
{% for next_name in next_levels %}
        self.{{ next_name }} = {{ next_name }}.Cfg(device, broker)
{% endfor %}
