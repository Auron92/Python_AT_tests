from stw_py_api_runtime.gen_classes.cmd_parse import TmReq
from stw_py_api_runtime.gen_classes.telemetry.parse import RepMessage, ReqMessage

{% for import in imports %}
{{ import }}{% endfor %}

messages_map = {
{% for message_id, message_desc in messages.items() %}
    {{ message_id }}: ('{{ message_desc[0] }}', {{ message_desc[1] }}),{% endfor %}
}
