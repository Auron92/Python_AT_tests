class {{ name }}:
{% for param in params %}
    {{ param.name }}: {{ param.param_type }}
{% endfor %}


@dataclass
class {{ name }}Harvester:
    def __init__(self, device_id, broker):
        self.device_id = device_id
        self.broker = broker

    def wait(self) -> {{ name }}:
        return {{ name }}(value='wait call')

    def ask(self) -> {{ name }}:
        return {{ name }}(value='ask call')