"""Переход во вложенные модули."""
from dataclasses import dataclass


@dataclass
class NextLevelItem(object):
    """Поля для доступа к "вложенным" элементам.

    Например, если есть прибор device = Mtcu(),
    то должен быть доступ к device.core.can, где device.core == gen_mtcu.Core()
    а device.core.can == gen_mtcu.core = gen_mtcu.core.Can()
    за создание device.core и device.core.can отвечают NextLevelItem.
    В классе будут 2 места:
    class SomeClass:
        {{ item_name }}: {{ class_name}}
    и
    def __init__(...:
        {{ item_name }} = {{ class_name }}()
    """

    class_name: str
    item_name: str
    import_string: str
